// This is a test file to test another method of turning the cube
// Take the first two pieces in the matrix and put them at the very end

function turnRight() {
    for (p of pieces) {
        console.log(p);
    }
}
